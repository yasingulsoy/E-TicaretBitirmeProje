@model Product
<div class="col mb-5">
    <div class="card h-100">
        <!-- Sale badge-->
        <div class="badge position-absolute" style="top: 0.5rem; right: 0.5rem">
            <form asp-action="Add" asp-controller="Favorites">
                <input type="hidden" name="ProductId" value="@Model.Id" />
                <button class="btn btn-transparent" type="submit" style="box-shadow:none">
                    <i class="bi-heart"></i>
                </button>
            </form>
        </div>
        <!-- Product image-->
        <a href="/Products/Details/@Model.Id">
            <img class="card-img-top" src="/Img/Products/@Model.Image" alt="@Model.Name">
        </a>


        <!-- Product details-->
        <div class="card-body p-4">
            <div class="text-center">
                <!-- Product name-->
                <h5 class="fw-bolder">@Model.Name</h5>
                <!-- Product price-->
                @* Fiyat formatı *@
                @Model.Price.ToString("c")
            </div>
        </div>
        <!-- Product actions-->
        <div class="card-footer p-4 pt-0 border-top-0 bg-transparent">
            <form asp-action="Add" asp-controller="Cart">
                <input type="hidden" name="ProductId" value="@Model.Id" />
                <div class="text-center">
                    <button class="btn btn-outline-dark mt-auto" type="submit"><i class="bi-cart-fill me-1"></i> Sepete Ekle</button>
                </div>
            </form>

        </div>
    </div>
</div>
